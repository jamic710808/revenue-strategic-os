# Revenue OS V8.0 Excel 模板使用說明

這份說明文件旨在幫助您了解如何準備及填寫 `Revenue_Import_Template_V8.xlsx`，以確保能順利將您的最新資料匯入至 **Revenue Strategic OS V8.0** 儀表板中。

## 🗂 模板基本結構

該 Excel 模板由 **5 個工作表 (Sheet)** 組成，每個工作表對應儀表板內不同的分析模組。
⚠️ **重要提醒：** 請勿修改工作表名稱 (Sheet Name)，否則系統將無法正確解析您的資料。

---

### 1. 工作表：`KPI總覽`
用於更新首頁「營收全境指揮部」及「財務 BP 路徑分析」的核心大指標。

| 欄位名稱 | 說明與範例 | 注意事項 |
| :--- | :--- | :--- |
| `指標名稱` | 不可修改的鍵值（例如：`Total Revenue`, `淨利潤率`, `活躍客戶數` 等） | 請勿新增或刪除列，也不要修改此欄文字。 |
| `數值` | 該指標的具體數字（例如：`1031.3`，如果是百分比請填小數如 `0.0497` 或直接打 `%` 計算後的值） | 根據指標性質填寫數字，純數值型態。 |
| `單位` | （選填）例如：`萬`, `元` 等。系統主要依賴內部格式化設定。 | 用於參考。 |
| `同期對比/狀態` | （選填）文字描述，例如：`▲ +140.14% YoY`, `健康水位` | 會覆蓋指標下方的狀態 Badge 文字。 |

---

### 2. 工作表：`月度趨勢`
用於更新「月度營收趨勢」圖表，包含 1 月到 12 月的各項設定。

| 欄位名稱 | 說明與範例 | 注意事項 |
| :--- | :--- | :--- |
| `月份` | `1月`, `2月` ... `12月` | 順序與文字請保持不變。 |
| `數值` | 該月營收數字，例如：`850`, `920` 等。 | 若只有部份月份有資料，未發生可填 `0`。 |
| `預測標記` | `TRUE` 或 `FALSE` | 若為 `TRUE`，該月份在柱狀圖會顯示為黃色 (警告/預測色)。 |
| `超標標記` | `TRUE` 或 `FALSE` | 若為 `TRUE`，該月份在柱狀圖會顯示為綠色 (成功色)。優先級大於預測標記。 |

---

### 3. 工作表：`財務BP`
用於更新「管理利潤表結構」表格。請輸入由上到下的財務結算科目。

| 欄位名稱 | 說明與範例 | 注意事項 |
| :--- | :--- | :--- |
| `科目` | `營業收入`, `營業成本`, `毛利` ... | 必須包含這幾個核心名詞以便計算。 |
| `金額(萬)` | 對應科目的金額（如營收 `1031.30`，成本填負數 `-670.35`） | 成本與費用請務必使用**負數**。 |
| `佔營收比` | 例如：`100%`, `65.0%`。 | 請填寫文字格式如 `100%` 或小數格式。 |
| `YoY` | 例如：`+140.14%`, `+128%`。 | 帶有正負號的百分比文字。 |
| `狀態` | 例如：`優`, `警`, `良` 或直接帶 Emoji `🟢 優秀` | 用於表格最右側標記。 |

---

### 4. 工作表：`產品分析`
用於更新「80/20 Pareto 貢獻度」圖表以及產品策略表格。

| 欄位名稱 | 說明與範例 | 注意事項 |
| :--- | :--- | :--- |
| `產品名稱` | 例如：`Flagship Model X` | 最多填寫 6~8 個主力產品，超過可能排版溢出。 |
| `營收佔比` | 該產品佔總營收比例（例如：`42.5%`） | |
| `ABC分類` | `A`, `B`, `C` | 影響 Badge 顏色展示（A=綠色, B=黃色, C=紅色）。 |
| `BCG象限` | `⭐ Star`, `🐄 Cash Cow` ... | 直接呈現在表格的象限欄位。 |
| `毛利率` | 該產品的毛利率 (例如：`38.2%`) | |
| `建議行動` | 短短一句話的戰略建議 (例如：`持續加碼行銷`) | |

---

### 5. 工作表：`RFM客戶`
用於更新「客戶 RFM 價值矩陣」中的 4 個重要分類數字與 4 個一般分類數字。

| 欄位名稱 | 說明與範例 | 注意事項 |
| :--- | :--- | :--- |
| `客戶類型` | `重要價值(111)`, `重要發展(101)` ... `流失客戶(000)` | 必須精確與此這 8 個類別對應。 |
| `客戶數` | 落在此分類的客戶人數 (例如：`2762`) | 整數。 |
| `佔比` | 佔總人數的比例 (例如：`27.66%`) | |
| `行動方針` | (選填) 對應的行銷動作 | |

---

## 🛠 匯入建議與錯誤排查

1. **僅修改資料，請勿刪除欄位與標題列。** 程式依賴第一列（A1, B1...）的欄位名稱來辨識資料。
2. **格式異常問題。** 如果匯入後儀表板某處顯示 "NaN" 或空白，通常是因為 Excel 儲存格被設定為純文字或其他無法轉換的格式，請確保「數值」欄位都是正規數字。
3. **沒有資料的狀況。** 如果某些項目（如新產品）還沒有資料，可以填寫 `0`。

準備好您的最新 Excel 分析結果後，請參考《Revenue_Import_操作說明.md》在網頁中點擊「📥 匯入資料」即可進行熱更新！
