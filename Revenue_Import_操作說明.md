# Revenue OS V8.0 儀表板資料匯入與操作說明

這份指南將帶領您如何在 **Revenue Strategic OS V8.0 Ultimate** HTML 儀表板中，將最新的業務分析數據透過 Excel 匯入，實現畫面指標的**動態更新**。

## 🎯 核心功能一覽

左側導覽列的底部新增了 **「📊 資料控制 (Data Control)」** 區塊，包含以下三個核心功能：

1. **📥 匯入資料 (Import Data)**：上傳填寫完畢的 Excel 檔案以覆蓋當前畫面。
2. **📋 下載模板 (Download Template)**：從當前預設數據中生成一份標準格式的 `.xlsx` 以供您修改。
3. **🔄 重置數據 (Reset to Default)**：清除您匯入的資料，將儀表板恢復至初始的預設狀態。

---

## 🚀 完整操作步驟

### 第一步：取得標準模板

1. 使用現代瀏覽器（建議 Chrome、Edge、Safari 或 Firefox）雙擊開啟 `Revenue_Strategic_OS_V8_0_Ultimate.html` 檔案。
2. 在左側導覽列最下方，找到 **[ 📋 下載模板 ]** 按鈕並點擊。
3. 系統會自動生成並下載一份名為 `Revenue_Import_Template_V8.xlsx` 的檔案。

### 第二步：利用 Power BI 匯出資料並填寫

1. 開啟剛剛下載的 Excel 模板。裡面包含了 **「KPI總覽」、「月度趨勢」、「財務BP」、「產品分析」、「RFM客戶」** 5 個工作表。
2. 根據您的最新業務狀況，或是從 Power BI 匯出的最新矩陣表資料，將對應的數字填入 Excel。
   > **補充說明**：具體每個欄位該如何填寫，請參閱另外一份文件 `Revenue_Template_使用說明.md`。
3. 填寫完畢後，儲存並關閉 Excel 檔案。

### 第三步：將新資料匯入儀表板

1. 回到 Revenue OS 的 HTML 頁面。
2. 點擊左側導覽列的 **[ 📥 匯入資料 ]** 按鈕。
3. 畫面上會彈出一個**深色的匯入對話框 (Modal)**。
4. 您有兩種方式上傳您的 Excel 檔案：
   - **拖曳上傳**：將儲存好的 `Revenue_Import_Template_V8.xlsx` 直接拖曳並放開在虛線框內。
   - **點擊選擇**：點擊虛線框，從系統檔案總管中尋找並選擇您的 Excel 檔案。
5. 選擇檔案後，虛線框下方會顯示您上傳的「檔名」與「檔案大小」。並且會自動啟動進度條與解析狀態。
6. 確認無誤後，點擊右下角的綠色 **[ 確認匯入 Update Dashboard ]** 按鈕。

### 第四步：檢視成果

- 對話框會自動關閉，此時您可以切換頂部的全境指揮部、財務BP、產品策略、RFM、區域分析、預測分析等分頁，您會發現所有您有修改過的 KPI 數值、長條圖比例、進度條百分比、以及表格內容，都已經**完全自動刷新**為您最新的資料！

---

## ↩️ 如何恢復原始資料？

如果您只是測試匯入功能，或者想看回原本的預設演示資料，請點擊左側導覽列的 **[ 🔄 重置預設 (Reset to Default) ]** 按鈕。儀表板會在一秒內閃爍並還原回初始的 `DEFAULT_DATA`。

## ⚠️ 常見問題 (FAQ)

**Q: 為什麼點擊匯入後，某些數字變成 "NaN" 或是整個儀表板破圖？**
A: 這通常是因為您在 Excel 的數值欄位中輸入了純文字或是無法計算的符號。請打開您的 Excel，確保如營收、成本、毛利率等欄位為**「純數字」或「小數」**，並移除多餘的字串後再重新匯入一次。

**Q: 為什麼我上傳了檔案，但只有首頁的數字變了，其他分頁沒變？**
A: 請確保您在 Excel 修改欄位時，**沒有動到工作表(Sheet)底部的名稱**。系統是透過名稱來配對更新邏輯的，如果名稱不對，該部分的更新就會被略過。

**Q: 該功能是否需要連接網路？**
A: 是的。為了減輕 HTML 檔案體積，解析 Excel 使用了外部開源函式庫 `SheetJS` 的 CDN。因此使用匯入、下載模板等功能時，您的設備必須**處於連網狀態**才能載入該功能庫。如果處於離線狀態，點擊匯入可能沒有反應。
